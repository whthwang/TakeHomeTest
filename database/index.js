// const mongoose = require('mongoose');
// mongoose.connect('mongodb://localhost/takeHome', { useNewUrlParser: true});
// const connection = mongoose.connection;

// connection.on('once', () => console.error('database failed to connect'));
// connection.on('open', () => console.log('database is connected!'))

// module.exports = { 
//   connection,
//   mongoose
// };